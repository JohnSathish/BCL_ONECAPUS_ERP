'use client';

import { LandingPage } from '@/components/landing/landing-page';

/** Public marketing home — always show landing; portal entry is via Sign In or Dashboard in the navbar. */
export default function HomePage() {
  return <LandingPage />;
}
