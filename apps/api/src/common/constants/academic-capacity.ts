/** Default seats for shared/common pool sections (AEC, MDC, SEC, VAC, VTC). */
export const DEFAULT_SHARED_POOL_SECTION_CAPACITY = 200;

/** Legacy Prisma default retained for migration / override detection. */
export const LEGACY_DEFAULT_POOL_CAPACITY = 40;
