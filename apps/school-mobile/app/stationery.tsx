import FeatureScreen from '@/ui/feature-screen';

export default function StationeryPage() {
  return (
    <FeatureScreen
      title="Stationery"
      body="School store items and requests will appear here when the office publishes them."
    />
  );
}
